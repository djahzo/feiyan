# 主播 B 站 UID（首页数据、舰长管理「从 B 站拉取大航海」等；需已开通直播间以解析 room_id）
BILIBILI_UID=

# 站点联系邮箱（公开展示，按需填写）
NEXT_PUBLIC_CONTACT_EMAIL=

# SQLite 数据文件（默认：本应用目录下 data/site.db）
# SQLITE_PATH=

# 后台会话 JWT 签名密钥：部署必须设置，≥16 字符，勿提交真实值到仓库。
# 生成示例：openssl rand -hex 32
ADMIN_SESSION_SECRET=

# 管理员账号与密码不在环境变量中配置。
# 首次部署请访问 /admin/setup，在页面中创建唯一管理员（仅当库中尚无管理员时可成功一次）。
