node_modules
.next
.env
.env.local
dist

.vercel

# Turbo 构建缓存与日志（构建产物，不该入库）
.turbo
**/.turbo

apps/web/public/pet-assets/backup/

# 运行时日志
*.log

# Claude Code 工具的本地配置（每台机器独立，不该入库）
.claude/settings.local.json
