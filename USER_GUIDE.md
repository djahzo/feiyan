# B站UP主展示网站 - 使用说明

## 🎉 项目已完成！

这是一个完全合法的B站UP主商务展示网站，使用B站公开API获取数据。

## ✅ 已实现的功能

### 1. UP主基本信息展示
- ✅ 头像、昵称、UID
- ✅ 个人签名
- ✅ 等级显示
- ✅ 粉丝数、关注数（通过公开API获取）

### 2. 直播状态检测 ⭐ 新功能
- ✅ 实时检测UP主是否在直播
- ✅ 显示直播间标题
- ✅ 直播中时显示醒目的直播提示卡片
- ✅ 点击直接跳转到直播间

### 3. 视频投稿列表
- ✅ 显示所有视频投稿
- ✅ 视频封面、标题、简介
- ✅ 播放量、评论数统计
- ✅ 发布时间显示
- ✅ **点击跳转到B站播放** 🎬

### 4. 动态展示（可选）
- ✅ 图文动态展示
- ⚠️ 可能需要登录（失败时不影响其他功能）

## 🔧 技术实现

### 使用的B站公开API（无需认证）

1. **用户基本信息**
   ```
   GET https://api.bilibili.com/x/space/acc/info?mid={UID}
   ```

2. **用户关系统计**
   ```
   GET https://api.bilibili.com/x/relation/stat?vmid={UID}
   ```

3. **直播状态检测** ⭐
   ```
   GET https://api.live.bilibili.com/room/v1/Room/get_status_info_by_uids?uids[]={UID}
   ```

4. **视频投稿列表**
   ```
   GET https://api.bilibili.com/x/space/wbi/arc/search?mid={UID}&ps=30&pn=1&order=pubdate
   ```

### 技术特点

✅ **完全合法** - 使用B站公开API，不涉及爬虫违法  
✅ **前端直接调用** - 浏览器直接请求B站API，避免服务器端限制  
✅ **智能缓存** - 5分钟缓存机制，减少重复请求  
✅ **频率控制** - 串行请求，避免触发频率限制  
✅ **错误处理** - 优雅降级，部分功能失败不影响整体  

## 🚀 如何使用

### 1. 启动项目

**后端**（可选，当前前端直接调用B站API）
```bash
cd backend
npm install
npm run dev
```

**前端**
```bash
cd frontend
npm install
npm run dev
```

### 2. 访问网站

打开浏览器访问：`http://localhost:5173`

### 3. 修改展示的UP主

编辑文件：`frontend/src/services/bilibili-direct.ts`

```typescript
// 修改这个UID为你想展示的UP主
const BILIBILI_UID = '14636839';
```

保存后，前端会自动热更新。

## 📊 数据来源说明

所有数据均来自B站公开API：
- ✅ 用户信息：公开可访问
- ✅ 视频列表：公开可访问
- ✅ 直播状态：公开可访问
- ⚠️ 动态列表：可能需要登录

## 🎯 使用场景

### 个人展示
- 展示自己的B站账号
- 作为个人主页的一部分
- 商务合作展示

### UP主推广
- 展示UP主的作品集
- 实时显示直播状态
- 方便粉丝查看投稿

### 数据分析
- 统计视频数据
- 分析投稿趋势
- 监控直播状态

## ⚠️ 注意事项

### 1. 频率限制
B站API有频率限制，建议：
- ✅ 使用缓存（已实现）
- ✅ 控制请求频率（已实现）
- ✅ 避免短时间内大量请求

### 2. 跨域问题
前端直接调用B站API可能遇到跨域问题：
- ✅ 浏览器环境通常可以正常访问
- ⚠️ 如果遇到跨域，可以使用CORS代理
- ✅ 或者使用后端代理（已提供）

### 3. 数据时效性
- ✅ 缓存时间：5分钟
- ✅ 刷新页面可获取最新数据
- ✅ 直播状态实时检测

## 🔐 合法性说明

### ✅ 完全合法的原因

1. **使用公开API** - B站官方提供的公开接口
2. **不破解认证** - 无需绕过任何安全机制
3. **正常访问** - 就像用户在浏览器中访问B站
4. **数据公开** - 所有数据都是公开可见的
5. **跳转到B站** - 给B站带来流量，而非截取流量
6. **遵守协议** - 控制请求频率，不对服务器造成负担

### ❌ 不涉及的违法行为

- ❌ 不破解API签名
- ❌ 不绕过登录验证
- ❌ 不爬取非公开数据
- ❌ 不用于商业盈利（个人展示）
- ❌ 不对B站服务器造成负担

## 📝 API接口详细说明

### 1. 获取用户信息
```bash
curl "https://api.bilibili.com/x/space/acc/info?mid=14636839"
```

**返回数据**：
- mid: 用户UID
- name: 昵称
- face: 头像URL
- sign: 个人签名
- level: 等级
- sex: 性别
- birthday: 生日

### 2. 获取直播状态
```bash
curl "https://api.live.bilibili.com/room/v1/Room/get_status_info_by_uids?uids[]=14636839"
```

**返回数据**：
- live_status: 0=未开播, 1=直播中
- room_id: 直播间ID
- title: 直播间标题
- cover: 直播间封面

### 3. 获取视频列表
```bash
curl "https://api.bilibili.com/x/space/wbi/arc/search?mid=14636839&ps=30&pn=1&order=pubdate"
```

**参数说明**：
- mid: UP主UID
- ps: 每页数量（最大50）
- pn: 页码
- order: pubdate(最新)/views(播放)/favorite(收藏)

**返回数据**：
- aid: 视频AV号
- bvid: 视频BV号
- title: 标题
- pic: 封面
- description: 简介
- created: 发布时间
- length: 时长
- play: 播放量
- video_review: 评论数

## 🎨 界面展示

### 主页面
- 用户信息卡片（头像、昵称、签名、等级）
- 直播状态卡片（直播中时显示）
- 视频投稿列表（网格布局）
- 动态列表（可选）

### 直播状态卡片
当UP主正在直播时，会显示醒目的直播提示：
- 🔴 红色"直播中"标签
- 直播间标题
- "进入直播间"按钮
- 渐变背景 + 动画效果

### 视频卡片
- 视频封面
- 标题（最多2行）
- 简介（最多2行）
- 播放量、评论数
- 发布时间
- 点击跳转到B站

## 🔄 更新日志

### v1.0.0 (2026-05-06)
- ✅ 实现用户信息展示
- ✅ 实现视频列表展示
- ✅ 实现点击跳转B站
- ✅ 添加直播状态检测 ⭐
- ✅ 前端直接调用B站API
- ✅ 添加缓存机制
- ✅ 添加频率控制
- ✅ 优化错误处理

## 📞 技术支持

如果遇到问题：

1. **检查网络连接** - 确保能访问B站
2. **清除缓存** - 刷新页面（Ctrl+F5）
3. **查看控制台** - 按F12查看错误信息
4. **等待限制解除** - 如果触发频率限制，等待10-30分钟

## 🚀 未来计划

### 短期计划
- [ ] 添加视频分类筛选
- [ ] 添加搜索功能
- [ ] 优化移动端显示

### 中期计划
- [ ] 添加数据统计图表
- [ ] 支持多个UP主对比
- [ ] 添加评论展示

### 长期计划
- [ ] 申请B站官方开放平台
- [ ] 实现完整的OAuth认证
- [ ] 添加更多数据分析功能

## 📄 许可证

MIT License

---

**注意**：本项目仅用于个人学习和展示，不用于商业目的。所有数据版权归B站和UP主所有。
